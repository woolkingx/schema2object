[package]
name = "schema-value"
version = "0.2.0"
edition = "2021"
description = "JSON Schema Draft-07 object wrapper — structure maps to accessors, logic maps to methods."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[dev-dependencies]
pretty_assertions = "1"
