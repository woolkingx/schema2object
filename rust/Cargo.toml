[package]
name = "schema2object"
version = "0.5.0"
edition = "2021"
description = "JSON Schema IS the object class. Zero dependencies."

[dependencies]
schema2object-macro = { path = "schema2object-macro" }

[dev-dependencies]
