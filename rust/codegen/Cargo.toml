[package]
name = "schema2object-codegen"
version = "0.2.0"
edition = "2021"
description = "Generated typed structs from JSON Schema — runtime + types in one file."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
unicode-segmentation = "1"
